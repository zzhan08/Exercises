package ThreadControls;

/*import java.util.concurrent.locks.Lock;
import com.google.common.util.concurrent.Striped;
public class ThreadStripedLocksExample {
  private Striped<Lock> stripedLocks = Striped.lock(10);	
  public static void main(String[]args) {
	   
  }
  public void update(ThreadStripedLocksExample t) {
	  Lock lock=stripedLocks.get(ThreadStripedLocksExample.getId());
	  lock.lock();
	  ThreadStripedLocksExample.add(null);
	  lock.unlock();
  } 
}*/